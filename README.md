# abbekeultjes.nl

## Development

This website is hosted on [GitHub Pages](https://pages.github.com/) as a **user site**. This means the root of the main branch must contain the build artifacts. Development is done on the `develop` branch. Run `pnpm build-and-deploy` to build and deploy the site to production (from local machine). See `deploy.sh` to see what happens.

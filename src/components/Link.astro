---
type Props = {
  href: string;
  openInNewTab?: boolean;
};

const { href, openInNewTab = false } = Astro.props;
const target = openInNewTab ? '_blank' : null;
const rel = openInNewTab ? 'noopener noreferrer' : null;
---

<!-- todo: add icon for external link -->
<a href={href} target={target} rel={rel} class="text-yellow-300 underline underline-offset-2"><slot /></a>
